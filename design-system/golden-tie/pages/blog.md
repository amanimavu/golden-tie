# Page Override: Blogs (`/blog`)

Inherits MASTER.md in full. New page — content and exact section breakdown not yet specified by
the brief.

## Known

- Shares site-wide Hero (with nav) and Footer.
- Not in the primary nav list given in the brief (About / Subsidiaries / CSR) — confirm whether
  Blogs is reachable only from the footer's secondary nav, or should also be a primary nav item.
- Per MASTER's Component Notes: blog post previews are the one place a boxed component may be
  "truly needed" — if so, keep it borderless per MASTER (differentiate with typography/whitespace,
  not a bordered/shadowed card).
- Likely implemented as an Astro content collection (see project `CLAUDE.md` — "Adding or
  managing content" guide) rather than hand-written pages per post.

## Open

- Listing page layout (chronological list vs. featured + grid) and individual post page template
  — not yet designed.
